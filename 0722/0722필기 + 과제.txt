윤영선선생님필기

6~7개정도로 모델링을 해서 인공지능 생성
(그거슨 마치 가내수공업입니다.)

- 영어 -
Deep Learning(깊게 학습)
 *왜 deep인가? -> 레이어의 깊이(레이어의 개수가 깊어짐..!)
 *정확도는 엄청높지만 시간이 오래걸림(양이 너무 방대해서)

Machine Learning(기계 학습)
 *기계를 학습시키는 모든행동

AI(인공지능)
위 3가지를 다해야함
AI안에 ML, DL / ML안에 DL
기계를 학습시키면서 기계는 학습한다

- 수학 -
함수 y= wx+b
이것을 어떻게 깔끔하게 만드냐
중학교와의 차이 => x와 y만 제공되므로 w와 b를 구해야함
weight = w(웨이트) / bias = b(바이어스)
실질적으로 최적의 weight값을 구하는것
반복적인 훈련으로 컴퓨터는 최적의 weight값을 찾아냄
-> x와 y를 줄테니 w를 구해라

주식예측하는것 또한 비슷함
로또번호의 빈도수로 예측하는것 또한 비슷..!

acc가 1.0인데 왜 predict는 일정하지 않고 다르게 나오는가?
-> 검색

model.summary() # param #에 나오는 값
= (입력값 * 그 전 레이어의 값) + 바이어스(입력값과 동일)
# param(line)의 개수로 생각해서 그림 그려서 그림개수를 따져본다.
[line수 + 바이어스]
# y = wx+b => wx = line / b = 바이어스 / y = param(파라메터값)
# 바이어스는 전지전능하여 모든곳에 다있다. 기존 노드수에 하나를
추가하는 느낌으로 계산
# 2*5 => 10 / 3*6 => 18 ...
# 머신을 할때 바이어스는 언제나 모든곳에 붙어서 진행된다
(파라메터의 숫자를 구할때)

******** 과제 ***********
x_train = 1~100
y_train = 501 ~ 600
x_test = 1001 ~ 1100
y_test = 1101 ~ 1200
y_predict 와 y_test가 유사하게 만들어라
이 데이터로 오늘 만든것 처럼 똑같이 만들어라..!
keras04_homework.py => 파일명
정제되지 않은 데이터를 스스로 정제해서 쓸만하게 만든후 처리한다
이거시 바로 데이터 전처리..?
