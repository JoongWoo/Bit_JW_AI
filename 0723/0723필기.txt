0723 윤영선 선생님 필기

지난과제 => 1400~1500대가 나오면 잘 한거임(bias때문에 그럴수밖에 없음)

와꾸(가로, 세로)는 중요하다. 행렬, 엑셀 등등
(가로)행은 무시된다 열(세로)이 우선이다!
행과 상관없이 열만 맞으면 된다

(1, ) => 몰라행 1열(컬럼, column)
-> 데이터 추가 삭제가 용이함

R2 - 결정계수
1에가까울수록 높다고 해석

		훈련(훈련용데이터)
x_train - y_train -> ML(train) -> 최적의 weight값

트레인을 하면서 스스로 훈련하면서 검증할수있게! val == validation
x_val - y_val -> ML(validation)
검증단계를 하나 더 넣어서 확실하게 하는것입니당

맞는가 테스트(테스트용 데이터)
x_test - y_test -> evaluate(평가) -> acc

예측용데이터
x3 -> predict => 새로운 y값구함 -> y_predict


잘라 - numpy에서 데이터 자르기
       - 리스트에서 자르기

1~60 - train
61~80 - val
81~100 - test

한줄이 아닌 범위 주석달기 범위시작에 ''' 범위끝에 '''

비는데이터 => 데이터가 많은 경우에는 행을 삭제해버릴수 있음
               => 새로운 값을 찾아 넣음

값을 할때 x가 여러개에서 y가 한개인것이 있다.
(캐글타이타닉도 여러가지요소(x)를 합쳐서 생존여부(y)한개를 뽑아냄)

원래의 데이터를 건들면 아니된다..!


